"use strict";
// // chatRouter.ts
// import express from 'express';
// import { ChatController } from '../controllers/chatController';
// const router = express.Router();
// const chatController = new ChatController();
// // Route to create a new conversation
// // router.post('/conversations', chatController.createConversation);
// // Route to create a new message
// router.post('/messages', chatController.createMessage);
// // Route to get all conversations for a user
// router.get('/conversations/:userId', chatController.getConversationsByUser);
// // Route to get all messages in a conversation
// router.get('/messages/:conversationId', chatController.getMessagesInConversation);
// export default router;
